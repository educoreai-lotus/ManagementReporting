name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

jobs:
  deploy_frontend:
    name: Deploy Frontend to Vercel
    runs-on: ubuntu-latest
    # only run if CI succeeded or manual dispatch
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    # do not fail the whole workflow if deploy fails
    continue-on-error: true
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Deploy to Vercel
        # uses the secrets already defined in the repo
        continue-on-error: true
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TEAM_ID: ${{ secrets.VERCEL_TEAM_ID }}
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "⚠️ No VERCEL_TOKEN provided — skipping deploy (Vercel will auto-deploy from GitHub)."
            exit 0
          fi

          vercel --prod --token "$VERCEL_TOKEN" --scope "$VERCEL_TEAM_ID" --yes || \
            echo "⚠️ Vercel deploy failed, falling back to Vercel auto-deploy."

  deploy_backend:
    name: Deploy Backend to Railway
    runs-on: ubuntu-latest
    # only run if CI succeeded or manual dispatch
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    # do not fail the whole workflow if deploy fails
    continue-on-error: true
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install Railway CLI
        run: npm install -g @railway/cli@latest

      - name: Deploy to Railway
        # uses RAILWAY_TOKEN and optionally RAILWAY_SERVICE_ID
        # Railway will auto-detect the service if RAILWAY_SERVICE_ID is not provided
        continue-on-error: true
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_SERVICE_ID: ${{ secrets.RAILWAY_SERVICE_ID }}
        run: |
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "⚠️ No RAILWAY_TOKEN provided — skipping deploy (Railway will auto-deploy from GitHub)."
            exit 0
          fi
          
          echo "$RAILWAY_TOKEN" | railway login --browserless || \
            echo "⚠️ Railway login failed, falling back to Railway auto-deploy."
          
          if [ -n "$RAILWAY_SERVICE_ID" ]; then
            railway up --service "$RAILWAY_SERVICE_ID" || \
              echo "⚠️ Railway deploy failed, falling back to Railway auto-deploy."
          else
            railway up || \
              echo "⚠️ Railway deploy failed, falling back to Railway auto-deploy."
          fi
